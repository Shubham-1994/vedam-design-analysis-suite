[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["mkdir -p uploads", "mkdir -p vector_db"]

[start]
cmd = "python run_backend.py"
